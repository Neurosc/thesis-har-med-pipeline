# Active configuration — edit these two values to switch the analysis pipeline.
# All scripts (Python, Julia, R) read from this file.

[active]
denoising_method = "GSR"   # options: "NoGSR" | "GSR"
atlas_method = "parcels"      # options: "spheres" | "parcels"
